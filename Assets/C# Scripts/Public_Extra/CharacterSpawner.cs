using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CharacterSpawner : MonoBehaviour
{
/*    public int spawnID = 1001;
    public Vector3 spawnPos = Vector3.zero;

    void Start()
    {
        //var info = TableMgr.Instance.Character.Get(spawnID);
        if (info == null)
        {
            Debug.LogError($"ID {spawnID}에 해당하는 캐릭터 정보 없음");
            return;
        }

        GameObject prefab = Resources.Load<GameObject>($"Prefabs/{info.Prefabs}");
        if (prefab == null)
        {
            Debug.LogError($"Resources/Prefabs/{info.Prefabs} 프리팹이 존재하지 않음");
            return;
        }

        Instantiate(prefab, spawnPos, Quaternion.identity);
    }*/
}

